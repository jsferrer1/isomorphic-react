describe('The QuestionList component', () => {

  beforeEach(() => {
    // console.log('Before each!')
  })

  beforeAll(() => {
    // console.log('Before all!')
  })

  it.only('should display a list of items', () => {
    expect(2+2).toEqual(4);
  });

  it.only('should display one item', () => {
    expect(2+2).toEqual(4);
  });
  
})
